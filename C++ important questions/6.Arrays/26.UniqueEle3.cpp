// Every element repeating 3 times and we have to find the  unique element[1,1,1,2,2,2,4]
#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main(){
     int n;
    cout << "Enter:";
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    for(int i=0;i<n;i++){
        for(int j=i+1;j<n;j++){
            if(arr[i] );
        }
    }
}
