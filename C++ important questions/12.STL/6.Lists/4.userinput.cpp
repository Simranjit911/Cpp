#include<bits/stdc++.h>.
// ll to take userinput
using namespace std;
class node{
    int data;
    node* next;

    node( int data){
        this->data=data;
        this->next=NULL;
    }
};
node* takeinput(){
  node* head=NULL;
  int data;
  cin>>data;
  if(data==-1)return;


   
}